import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MusicPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MusicPage(title: 'Music')));
    //  final titleFinder = find.text('Music');
    //  expect(titleFinder, findsOneWidget);
  });
}
